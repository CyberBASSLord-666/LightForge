# Post-failure observer: original gate failed, later readiness observed

Diagnostic run [34426586708](https://github.com/CyberBASSLord-666/LightForge/actions/runs/34426586708) used source `92d8392140e8cfa3fd2980df10695a66629f11ef`. This sixth history preserves the original failed background result. It is supplementary evidence and cannot authorize its diagnostic APK for release.

| Result | Observed scope |
| --- | --- |
| Background lifecycle: **failed** | Studio separation, neural analysis and choreography completed under screen-off/Doze and saved the project in 370.700 seconds (native passage: 314.340 seconds). Completed-project reopening exceeded the unchanged 45-second readiness deadline. The suite did not continue into Balanced, Cancel/Resume or timeout checks. |
| Additional observation: **ready after 6.225 seconds** | With the original failure frozen, the same visible WebView satisfied all original JavaScript/native predicates, visual-state callback and hardware frame commit 6.225 seconds into the separate observation, approximately 51.2 seconds after the original wait began. The observation ended at 6.329 seconds; it never changed the failed gate or resumed the suite. |
| Native report capture: **complete** | The production native export API returned 33,485 bytes. Two bounded chunks (32,768 and 717 bytes) reconstructed the exact report with verified SHA-256, byte count, UTF-8 and report boundaries. No renderer response was required for this capture. |
| Diagnostics: **passed, 8 checks** | The separate crash/recovery/export suite passed all eight original checks. This remains independent of the failed background suite. |

The original probe stopped receiving JavaScript responses at 17.642 seconds: composition and background restoration were active, the preview model was not ready and no canvas frames had been rendered. During additional observation, all JavaScript readiness flags were true by 4.324 seconds and the probe awaited the hardware frame. At 6.225 seconds the model was ready, two frames were recorded, composition/restoration flags were idle, the view remained visible and focused, and the hardware frame was committed. No application settings, quality, models, view visibility, resolution or original gate deadlines were changed by the observer.

The native report contains no ERROR/FATAL entries, zero dropped events and zero write failures. It reports `lowMemory=false`, approximately 2.46 GB available device memory, and a normally completed analysis job. The journal records verification of the saved arrangement followed by two existing preview blur-size warnings, with no digest mismatch or restore error. The Android last-ANR snapshot says no ANR occurred since boot. Main-thread watchdog evidence records a temporary HWUI wait and subsequent responsiveness. These observations establish late completion on this emulator, not a persistent deadlock or a precise attribution to one rendering substage. They do not establish physical-phone performance.

The original 45-second failure is copied unchanged in the background receipt, explicit failure marker and every observation. The observer had an independent maximum 90-second budget; native export had a maximum 20-second capture budget, with a 2 MiB report bound and 32 KiB chunks. The frozen harness receipt records the exact Java, runner, workflow and 12-test source hashes. Those sources remain available at diagnostic commit `92d8392140e8cfa3fd2980df10695a66629f11ef`; the release harness was restored separately and is not replaced by this history.

The tested APK retains exactly the three reviewed web replacements over the pinned earlier CI APK. All 124 other non-signature entries preserve their metadata and compressed/uncompressed hashes; all 127 payload contents match corrected-fixture run `34424613238`. The provenance identifies the new temporary signing identity, `update_compatible: false`, `diagnostic_only: true` and `release_eligible: false`.

Separately, full production verification run [34424617050](https://github.com/CyberBASSLord-666/LightForge/actions/runs/34424617050), source `57e6ee996bd8f9d135e89334585efe90d8667b68`, passed its verification and Android jobs with the original 45-second gate. Its current source-bound Android receipt has six complete lifecycle checks and its diagnostics receipt has eight checks. That distinct full run supplies the production evidence; this observer's failed result is not rewritten or substituted for it.

Original records were copied byte-for-byte from final artifact **10133270877**, ZIP SHA-256 `4d00d50718d6bace0c685503600d0b4a22b6890a22bbfac95b748d96b9b7ba1c`. Early artifact **10133234091**, ZIP SHA-256 `fd0b3a37b4dc44464622fb9f172e0af94b61ba7177989ea85d316350b0160dc6`, has identical overlapping background, observer, report and provenance records. Both downloads were checksum-verified. `preservation-manifest.json` records each original path, byte count and SHA-256, along with the independent payload/transport audit. `artifact-metadata.json` records fetched GitHub API metadata and verification results. The two large full platform logcats remain in the verified artifact and extracted archive; their original sizes and hashes are also recorded in the manifest. All older histories remain unchanged.
