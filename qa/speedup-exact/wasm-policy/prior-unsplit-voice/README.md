Run `python3 qa/speedup-exact/wasm-policy/verify.py` to record current worker/analyzer routing tests and the GAME pool nonshipping decision.

The suite executes the actual worker and GAME adapter using small mocked model outputs. It exercises Android core/isolation/shared-memory boundaries, the preserved browser policy, cached restoration, runtime downgrade, real app bridge detection, and two nonzero transcription passages with all eight steps each. An eligible pool-capacity callback and child-worker trap prove that the release's disabled activation cannot launch the experiment.

This is behavioral evidence only. Real model output identity and actual Android shared-memory execution require their separate source-bound receipts. The historical pool, memory-admission and resource-policy receipts retain their original source scope; their previous passing fields do not qualify shipping pooling.
