const ort=require('../web/analysis/vendor/ort.wasm.min.js'),fs=require('fs'),path=require('path');
(async()=>{ort.env.wasm.numThreads=1;ort.env.wasm.wasmPaths=path.resolve(__dirname,'../web/analysis/vendor')+'/';const bytes=fs.readFileSync(path.resolve(__dirname,'../web/analysis/models/beatnet-v1.onnx'));const session=await ort.InferenceSession.create(bytes,{executionProviders:['wasm']});const b=fs.readFileSync(path.resolve(__dirname,'reference_features80.f32'));const f=new Float32Array(b.buffer,b.byteOffset,b.length/4);const out=await session.run({features:new ort.Tensor('float32',f,[1,80,272]),h:new ort.Tensor('float32',new Float32Array(300),[2,1,150]),c:new ort.Tensor('float32',new Float32Array(300),[2,1,150])});fs.writeFileSync(path.resolve(__dirname,'wasm_probs80.f32'),Buffer.from(out.probabilities.data.buffer));console.log({onnxWasm:'PASS',outputs:Object.fromEntries(Object.entries(out).map(([k,v])=>[k,v.dims]))});await session.release();})();
