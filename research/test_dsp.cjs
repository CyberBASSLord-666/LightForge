const fs=require('fs'),path=require('path'),assert=require('assert');require('../web/analysis/dsp.js');
const root=path.resolve(__dirname,'..'),cfg=JSON.parse(fs.readFileSync(root+'/web/analysis/models/features.json'));let b=fs.readFileSync(root+'/research/test_audio22050.f32'),audio=new Float32Array(b.buffer,b.byteOffset,b.length/4);const padded=new Float32Array(80*441+1411);padded.set(audio.subarray(0,padded.length-705),705);const f=new LightForgeDSP.FeatureExtractor(cfg).extract(padded,0,80);b=fs.readFileSync(root+'/research/reference_features80.f32');const ref=new Float32Array(b.buffer,b.byteOffset,b.length/4);let max=0,rms=0;for(let i=0;i<ref.length;i++){let e=Math.abs(f.features[i]-ref[i]);max=Math.max(max,e);rms+=e*e;}rms=Math.sqrt(rms/ref.length);assert(max<1e-5,`Feature error${max}`);const silence={duration:10,beat:new Float32Array(500),down:new Float32Array(500),bass:new Float32Array(500),mid:new Float32Array(500),high:new Float32Array(500),rms:new Float32Array(500),colour:new Float32Array(1500)};const result=LightForgeDSP.summarize(silence);assert.equal(result.bpm,0);assert.equal(result.beats.length,0);assert.equal(result.sections.at(-1).end,10);console.log(JSON.stringify({featureMaxAbsError:max,featureRMSError:rms,silenceTest:'pass'}));
