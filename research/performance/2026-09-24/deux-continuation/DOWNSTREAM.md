Prepared Deux downstream continuation — not yet executed

`deux_downstream.py` must run after `deux_retry.py` finishes and all GAME jobs stop. It requires the resulting `DX_RUN`, `DX_REPO`, `DX_APK`, `DX_AUDIO`, `DX_SOURCE_HASHES`, `WORK` and `TOOLCHAIN` globals. Qualification must have exited 1 with `NUMERICAL_EQUIVALENCE_UNPROVEN`, all inputs rechecked, exact CPU/ALL and CUDA/BASIC observer pairs, and their four complete output files. Any missing requirement fails closed and retains a diagnostic archive.

The wrapper reads frozen source commit `8590ac4a67de4340857a96ffe38bae53d7d902b8`, tree `c841bf18f91d3d04dd6daf7e886e2aa473ab8c70`. It does not modify the existing source checkout, application, manifests, comparator, or parent evidence. A separate source snapshot contains 25 exact Git blobs. Original GAME Large graphs, Frame-MN10 and ONNX Runtime Web 1.20.1 files are recovered from the already verified public LightForge 2.3.1 APK and checked against the frozen application asset inventory. No npm install or replacement model is involved.

It downloads the official Node 22.19.0 Linux x64 archive over HTTPS, verifies SHA256 `c0649af18e6a24f6fe5535a3e86b341dd49a8e71117c8b68bde973ef834f16f2`, and extracts only its regular `bin/node` file into a fresh toolchain directory. The checksum was obtained from the [official August 28, 2025 release page](https://nodejs.org/en/blog/release/v22.19.0). The binary reports its exact version before inference. This is a reproducibility pin, not a claim that it is the latest release.

The unchanged comparator replays the captured first Deux passage through the original separator consumer, emits the first committed five-second chunk and deliberately stops when the missing second passage is requested. Both bounded excerpts then pass through the actual Frame-MN10, vocal-detail, GAME Large (all eight steps), and fusion consumers. The existing physical-unit sensitivity thresholds and nonempty coverage checks remain unchanged. A failure or insufficient coverage is retained; no tolerance is invented.

The private process group is retired on completion or interruption. Parent input, source, model/vendor, Node binary and existing-checkout integrity are checked again after inference. An immutable ZIP is created with closed logs, both captured/profiled pairs, parent receipt/setup, bound source, downstream outputs and receipts. Model weights, APK, Node archive/binary and credentials are excluded. `export_deux_downstream.py` verifies the ZIP hash before downloading it. The full upstream Deux ZIP must also be retained separately because this archive is a downstream diagnostic, not the complete accelerator provenance.

A passing result is only `EXCERPT_SENSITIVITY_PASS`. It cannot establish full-song musical quality, second-passage overlap/add behavior, full-source vocal context, CUDA downstream performance, Android behavior, or 75% complete-analysis improvement. It does not resolve the original exact-output gate or admit benchmark ratios. Quality, release, timing and 75% approval flags stay false.

Preparation validation: both Python scripts compile; all 25 frozen Git blobs, 14 consumed module/config inventory pins and six GAME manifest asset pins match. The models have not been run by this preparation task.
